meta {
  name: login
  type: http
  seq: 1
}

post {
  url: http://localhost:5198/api/v1/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "admin@localhost",
    "password": "changeme"
  }
}

settings {
  encodeUrl: true
}
